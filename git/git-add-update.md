The `git add -u` (or `--update`) command stages changes from tracked files only. It updates the index by matching the current state of tracked files in your working directory.

- It automatically stages files you have removed from your project
- It stages all edits made to files already known by `git`
- It will not stage "untracked" files
