IP address are unique identifiers used to determine who i who on the internet. IP addresses can be formatted differently depending on whether they use `IPv4` or `IPv6` protocol.

'IP' stands for Internet Protocol, which is the set of rules that make it possible for devices to communicate over the internet.
